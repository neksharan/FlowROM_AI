# Reduced-order modeling of fluid flows using autoencoder and transformer networks

The code here uses autoencoder and transformer networks to model the velocity/pressure field in fluid flows. The current implementation models the flows shown below. The list will be updated as data from more flow fields are used for training/testing.

## Flow over an airfoil at an AoA of 30˚


## Jet flow

<img src="images/atmp_turb_contourf_fast_short.gif?raw=true&v=100" alt="your_alternative_text" width="100%" loop="true" autoplay="true">


